# === SERVER CONFIG ===
server.port=8080

# === DB CONFIG ===
spring.datasource.url=jdbc:mysql://localhost:3306/classe_note_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=classe_note_db
spring.datasource.password=abda
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === JPA / Hibernate ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# === JWT CONFIG ===
# jwt.secret and jwt.expiration are not standard Spring Boot properties.
# Configure these in your custom configuration class or use application-specific properties.

# === Other ===
spring.application.name=classe-note